echo "🔍 Запуск ESLint..."
npm run lint

echo "✨ Форматування Prettier..."
npm run prettier:check
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"
echo "🔍 Запуск ESLint..."
npx eslint . --fix

echo "✨ Форматування Prettier..."
npx prettier --write .

echo "✅ Перевірка завершена, додаємо зміни..."
git add .